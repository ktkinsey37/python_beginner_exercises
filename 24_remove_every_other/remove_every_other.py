def remove_every_other(lst):
    """Iterates through a list and if the index number is even, append those items in a new list.
    Return the new list of every other item.
    """
    output = []
    count = 0
    for item in lst:
        if count % 2 = 0:
            output.append(item)
    return output

